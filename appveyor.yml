image: Visual Studio 2019

environment:
  matrix:
    - nodejs_version: "20"

build: off

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install --legacy-peer-deps


test_script:
  - npm test
